[package]
name = "posix-tests"
version = "0.1.0"
nanvix-version = "0.16.140"

[builds]
[builds.matrix]
platforms = ["hyperlight", "microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["128mb"]
